/**
 * عکس‌های پروفایل کانال‌ها و تبلیغ‌کننده‌ها
 * -------------------------------------------------
 * این فایل عمداً کاملاً از app.js جداست: عکس پروفایل هر کانال/تبلیغ‌کننده
 * فقط و فقط با ویرایش همین یک فایل عوض می‌شود، بدون هیچ دست‌زدنی به منطق
 * برنامه در app.js.
 *
 * هر کلید همان photoKey ای است که در آرایه CHANNEL_PROMOS (داخل app.js) به
 * هر کانال یا تبلیغ نسبت داده شده.
 *
 *   photo: 'images/xxx.jpg'  → عکس واقعی پروفایل. ابعاد پیشنهادی: مربعی،
 *                              حدود ۳۰۰×۳۰۰ پیکسل (چون همیشه داخل یک قاب
 *                              کوچک و گرد نمایش داده می‌شود، عکس بزرگ‌تر فقط
 *                              حجم صفحه را بی‌دلیل بالا می‌برد).
 *   photo: null               → یعنی هنوز عکسی برای این کانال ثبت نشده؛
 *                              به‌جای عکس، همان آیکون طراحی‌شده‌ی SVG (از
 *                              PROMO_ICONS در app.js) نمایش داده می‌شود.
 *                              اگر عکس هم ثبت شود ولی لود آن با خطا مواجه
 *                              شود (لینک خراب/حذف‌شده)، باز هم به‌طور خودکار
 *                              به همان آیکون SVG برمی‌گردیم؛ یعنی کاربر هیچ‌وقت
 *                              جای خالی یا آیکون شکسته نمی‌بیند.
 *
 * برای اضافه یا عوض کردن یک عکس:
 *   ۱. فایل عکس را داخل پوشه‌ی images/ بگذار (ترجیحاً مربعی و کوچک، چیزی
 *      حدود ۳۰۰×۳۰۰ پیکسل کافی است).
 *   ۲. مسیرش را همینجا جلوی کلید مربوطه بنویس. همین — نیازی به تغییر جای
 *      دیگری از کد (app.js/index.html) نیست.
 *
 * برای افزودن یک کانال/تبلیغ‌کننده‌ی کاملاً جدید:
 *   ۱. یک کلید تازه اینجا اضافه کن، مثلاً: my_channel: { photo: 'images/my-channel.jpg' }
 *   ۲. همان کلید را به‌عنوان photoKey در آبجکت مربوطه داخل CHANNEL_PROMOS (app.js) بنویس.
 */
const CHANNEL_PHOTOS = {
    avay_khiyal: { photo: 'images/avay_khiyal.webp' },
    tech_nour: { photo: 'images/tech_nour.webp' },
    rasa_meme: { photo: 'images/rasa_meme.webp' },

    // عکس پروفایل تبلیغ‌کننده‌ی فعلی. برای هر تبلیغ جدید، یک کلید تازه با
    // همین الگو اضافه کن و photoKey همان تبلیغ (در CHANNEL_PROMOS، داخل
    // app.js) را به همین کلید اشاره بده. تا وقتی عکس واقعی تبلیغ‌کننده را
    // نداری، همین‌جا null بگذار تا آیکون SVG جایگزینش شود.
    im_azad: { photo: 'images/im_azad.webp' }
};
